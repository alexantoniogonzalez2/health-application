(function(){
  var app = angular.module('presupuesto', ['templates', 'ng-rails-csrf']);

  app.controller('PresupuestoController', ['$http','$filter', function($http, $filter){

    var pagos = this;
    this.cargarPagos= function(presupuesto) { 

      $http.get('/cargar_pagos', { params: { atencion_salud_id: atencion_salud_id, pres: presupuesto } }).success(function(data){
        pagos = data;
      });

    };

    
    
  } ]);


})();

if ( $.fn.dataTable.isDataTable( '#lista_presupuestos' ) ) { }
else {
  $('#lista_presupuestos').DataTable({
    "lengthMenu": [[10, 25, 50, -1], [10, 25, 50, "Todos"]],    
    "columnDefs":[ { "targets": [ 0 ], "visible": false }],
    "language": {
      "lengthMenu": "Mostrar _MENU_ presupuestos por página",
      "zeroRecords": "La búsqueda no arrojó resultados.",
      "info": "_START_ a _END_ de _TOTAL_",
      "infoEmpty": "No hay presupuestos que mostrar",
      "infoFiltered": "(filtrados de _MAX_)",
      "search": "Búsqueda",
      "oPaginate": {
        "sPrevious": "Página anterior",
        "sNext": "Página siguiente"
      }
    }
  });
}